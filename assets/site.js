(()=>{let lastTrigger=null;const modal=document.getElementById("download");const open=trigger=>{lastTrigger=trigger;modal.hidden=false;document.body.classList.add("modal-open");modal.querySelector("[data-close]").focus()},close=()=>{modal.hidden=true;document.body.classList.remove("modal-open");lastTrigger?.focus()};document.querySelectorAll('[data-open="download"]').forEach(button=>button.addEventListener("click",()=>open(button)));modal.querySelector("[data-close]").addEventListener("click",close);modal.addEventListener("click",event=>{if(event.target===modal)close()});document.addEventListener("keydown",event=>{if(event.key==="Escape"&&!modal.hidden)close()})})();
