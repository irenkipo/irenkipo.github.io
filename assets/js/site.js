(()=>{"use strict";const q=selector=>document.querySelector(selector),qa=selector=>[...document.querySelectorAll(selector)],body=document.body,nav=q("#site-nav"),toggle=q(".nav-toggle"),focusable="a[href],button:not([disabled]),input:not([disabled]),select:not([disabled]),textarea:not([disabled]),[tabindex]:not([tabindex=\"-1\"])";let opener=null;toggle?.addEventListener("click",()=>{const open=toggle.getAttribute("aria-expanded")==="true";toggle.setAttribute("aria-expanded",String(!open));nav.classList.toggle("open",!open)});nav?.querySelectorAll("a").forEach(link=>link.addEventListener("click",()=>{toggle.setAttribute("aria-expanded","false");nav.classList.remove("open")}));const closeModal=modal=>{if(!modal||modal.hidden)return;modal.hidden=true;body.classList.remove("modal-open");const target=opener;opener=null;target?.focus()};const trapFocus=(event,modal)=>{const items=qa(focusable).filter(element=>modal.contains(element)&&element.getClientRects().length);if(!items.length){event.preventDefault();return}const first=items[0],last=items[items.length-1];if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus()}};qa("[data-open]").forEach(button=>button.addEventListener("click",()=>{const modal=document.getElementById(button.dataset.open);if(!modal)return;opener=button;modal.hidden=false;body.classList.add("modal-open");modal.querySelector("[data-close]")?.focus()}));qa(".modal").forEach(modal=>{modal.querySelector("[data-close]")?.addEventListener("click",()=>closeModal(modal));modal.addEventListener("click",event=>{if(event.target===modal)closeModal(modal)});modal.addEventListener("keydown",event=>{if(event.key==="Tab")trapFocus(event,modal)})});document.addEventListener("keydown",event=>{if(event.key!=="Escape")return;const modal=q(".modal:not([hidden])");if(modal){event.preventDefault();closeModal(modal)}})})();
